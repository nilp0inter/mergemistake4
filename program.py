if user.is_friend():
    # Friend behavior
    user.greet()  # Good manners after all
    user.say_goodbye()

if user.is_enemy():
    # Enemy behavior
    user.greet()  # Good manners after all
    user.say_goodbye()
