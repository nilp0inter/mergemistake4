if user.is_enemy():
    # Enemy behavior
    user.greet()  # Good manners after all
    user.stab_to_death()
    user.say_goodbye()

if user.is_friend():
    # Friend behavior
    user.greet()  # Good manners after all
    user.hug_with_love()
    user.say_goodbye()
